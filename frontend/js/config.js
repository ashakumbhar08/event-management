/* ===================================
   EventHub - Configuration Module
   
   This file stores shared configuration for the entire frontend.
   Change API_BASE here if the backend URL changes (e.g. during deployment).
   
   For college project - beginner friendly code
   =================================== */

// API Base URL - points to the backend server
// Change this URL when deploying to production
const API_BASE = "http://localhost:5001/api";

// Export for use in other files (if using modules)
// For now, this is a global variable accessible everywhere
